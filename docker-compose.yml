services:
  backend:
    build: ./backend
    ports:
      - 5000:5000
      - 51212:51212
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    stdin_open: true
    tty: true
    depends_on:
      - backend
